# ==============================================================================
# DFF-MambaNet — Python dependencies
# ==============================================================================
# Tested with Python 3.11 on Google Colab, Ubuntu 22.04/24.04, and Windows 11.
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE on `mamba-ssm`: it requires a CUDA-enabled GPU and a matching CUDA
# toolkit/PyTorch build to compile its custom kernels, and is NOT required to
# run this project — without it, the Vision Mamba branch automatically uses
# the pure-PyTorch fallback implementation in `models/vision_mamba.py`
# (slower, but numerically equivalent and fully functional on CPU/Windows/
# any GPU). Install it only if you have a CUDA GPU and want the faster
# official kernel. It is therefore listed as commented-out below — uncomment
# it on a CUDA-enabled Linux machine if desired.
# ==============================================================================

# --- Core deep learning stack -------------------------------------------------
torch>=2.1.0
torchvision>=0.16.0

# --- Vision Mamba (optional, CUDA-only — see note above) ---------------------
# mamba-ssm>=1.2.0
# causal-conv1d>=1.1.0          # required by mamba-ssm

# --- Data handling / augmentation ---------------------------------------------
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0
albumentations>=1.4.0
pandas>=2.0.0
pillow>=10.0.0

# --- Modeling utilities --------------------------------------------------------
timm>=0.9.12
scikit-learn>=1.3.0

# --- Training / experiment tracking -------------------------------------------
tensorboard>=2.15.0
tqdm>=4.66.0

# --- Visualization --------------------------------------------------------------
matplotlib>=3.7.0
